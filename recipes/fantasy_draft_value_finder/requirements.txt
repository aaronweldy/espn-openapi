# This recipe uses the ESPN API client models from the main project
# No additional dependencies required beyond the main requirements.txt

# Core dependencies (from main project):
# - httpx
# - python-dateutil
# - attrs

# Optional for CSV export:
# csv module is part of Python standard library